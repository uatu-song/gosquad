# Canon layer — Book 1 FIRST EDITION (quarry)

Measurement scaffolding for the published first edition, so the series style
rules can map the prose disease per chapter (the 9x fragment escalation etc.).
NOT a canon book: the rebuild (canon/book_1/) is the future edit-of-record.

## Ledger status (GOSQUAD_KIT editorial operation, from 2026-08-29)

This directory doubles as the operation's ledger. See the mapping table at the top of
`DECISIONS_LOG.md` — the kit's `ledger/` directory was deliberately not created.

| file | status |
|---|---|
| `DECISIONS_LOG.md` | live — house style + destination ruled 2026-08-29 |
| `CHAPTER_INDEX.yaml` | populated — POV determined for all 30 chapters |
| `STYLE_PROFILE.md` | populated — baseline measurements, descriptive only |
| `CH01-11_TOPOGRAPHY.md` | populated — vetted world-map for cold agents (reads at ~1.6k words instead of the 23.5k-word corpus) |
| `CANON_FACTS.jsonl` | **stub** — Pass 7 harvest populates it |
| `PROMISES.jsonl` | **empty** — Pass 7 harvest populates it |
| `CHRONOLOGY.jsonl` | **empty** — Pass 7 harvest populates it |

An empty ledger here means NOT YET HARVESTED, never "nothing to report."
